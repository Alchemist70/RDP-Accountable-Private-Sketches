# Requirements for the privacy_enhanced_fl experiments
# NOTE: TensorFlow and TensorFlow Federated require Python 3.10 or 3.11 as of current stable releases.
# If you are using Python >= 3.12 (or 3.13), the tensorflow / tff lines will fail; create a venv with Python 3.10/3.11

# Core ML frameworks (TensorFlow stack - only for Python 3.10/3.11)
tensorflow==2.12.0; python_version < "3.12"
tensorflow-federated==0.42.0; python_version < "3.12"

# PyTorch stack (works for many Python versions)
torch>=2.1.0
torchvision>=0.15.0

# Differential privacy libraries
opacus>=1.1.0
tensorflow-privacy>=0.9.0; python_version < "3.12"

# Utilities and scientific stack
numpy>=1.23.0
scipy>=1.9.0
pandas>=1.5.0
scikit-learn>=1.1.0
matplotlib>=3.6.0
seaborn>=0.12.0
scikit-image>=0.19.0

# Optional cryptography / secure aggregation helpers
cryptography>=39.0.1

# Jupyter and tooling
jupyterlab
notebook

# Helpful: pinned versions can be adjusted; if installation fails for TF/TFF, recreate the venv with Python 3.10 or 3.11
